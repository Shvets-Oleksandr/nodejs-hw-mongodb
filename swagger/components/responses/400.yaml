description: Bad request error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 400
        message:
          type: string
          example: 'Something went wrong'
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: '67940f725614cfc not valid id'
